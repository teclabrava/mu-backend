APP_URL=https://${STAGE}.teclabrava.com
CORS_ALLOW_ORIGINS=https://${STAGE}.teclabrava.com
APP_ENV=${STAGE}
APP_DEBUG=true
DYNAMODB_PREFIX=${STAGE}
DYNAMODB_CONNECTION=aws
DYNAMODB_REGION=${AWS_REGION}
DYNAMODB_KEY=${AWS_ACCESS_KEY_ID}
DYNAMODB_SECRET=${AWS_SECRET_ACCESS_KEY}
BUCKET_KEY=${AWS_ACCESS_KEY_ID}
BUCKET_SECRET=${AWS_SECRET_ACCESS_KEY}
BUCKET_REGION=${AWS_REGION}
BUCKET_NAME=manu-frontend-${STAGE}
BUCKET_URL=https://${STAGE}.teclabrava.com
