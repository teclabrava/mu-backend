version: "3"
services:
  api:
    container_name: api
    build: .
    ports:
      - "8080:8080"
    command: |
      ./init.sh
    volumes:
      - ./.env.example.local:/app/.env
      - ./storage/bucket:/app/storage/bucket
